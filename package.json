{
  "name": "codex-model-router",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Extensible local model router for the Codex App with Kimi and DeepSeek",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "check": "node scripts-check.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "proper-lockfile": "4.1.2"
  }
}
