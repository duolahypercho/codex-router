{
  "name": "codex-model-router",
  "version": "0.4.0-beta.1",
  "private": true,
  "type": "module",
  "description": "Extensible local model router for Codex and experimental Claude Desktop targets",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "check": "node scripts-check.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "proper-lockfile": "4.1.2"
  }
}
