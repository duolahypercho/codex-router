#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
case "${MODEL_ROUTER_TARGET:-codex}" in
  codex) exec node "$repo_dir/src/doctor.mjs" "$@" ;;
  claude) exec node "$repo_dir/src/claude-doctor.mjs" "$@" ;;
  *) echo "MODEL_ROUTER_TARGET must be codex or claude." >&2; exit 2 ;;
esac
