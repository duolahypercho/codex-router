- **Azure Sol can delegate tasks to configured Luna subagents across the provider boundary.** Azure-origin encrypted collaboration arguments previously failed before the child could run a tool because another backend could not decrypt them. Azure Responses collaboration messages now use Codex's plaintext handoff, with the client-visible namespace restored and an omitted child model left to the configured default. Use the `collaboration` tool namespace with current Codex versions so plaintext delivery and redaction take the supported path.
