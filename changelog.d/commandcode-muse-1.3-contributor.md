- **Command Code's cheaper Muse Spark 1.3 Contributor tier is now a routed
  model.** Command Code's Provider API lists `meta/muse-spark-1.3-contributor`
  beside the full 1.3 route, so the registry now carries
  `commandcode/muse-spark-1.3-contributor` with the same 1M context window,
  900K auto-compact, and text-plus-image input as its sibling. It follows the
  Command Code Muse precedent rather than Meta's Responses ladder, so it
  advertises only the `high` effort and no reasoning summaries. Meta may use
  this tier's inputs and outputs for training.
