- **Control Center browser sign-in reliably reports an OAuth child that exits during browser handoff.** Keep the detached login child referenced until the browser opener succeeds, so Windows Node can deliver its close event even if sign-in ends while the opener is pending. The custom-endpoint and harness regression fixtures now use their checked-out router version instead of a separately installed older build.
