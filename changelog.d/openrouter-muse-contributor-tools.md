- **Muse Spark 1.3 Contributor through OpenRouter accepts Codex's tool surface.**
  Meta rejected an 80-character tool name and then a recursive JSON Schema,
  both surfaced by OpenRouter as `HTTP 400: Provider returned error`. This
  exact route now uses the existing reversible 64-character tool aliases and
  cycle-closing schema repair. Tool history and returned calls retain their
  client identities; other OpenRouter models keep their existing behavior.
